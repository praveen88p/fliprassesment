void print(int ind, vector<int> &ds, int arr[] , int n){
if(ind == n){
for(auto it : ds){
cout << it << " ";
}
if(ds.size() == 0){
cout <<endl;
return ;
}

printF(int+1, ds, arr, n);

ds.push_back(arr[int]);
printF(int +1, ds, arr, n);
ds.pop_back();
